// Copy this file to config.js and set the local server port for the desktop app.
// The browser extension talks to the app via DESKTOP_APP_URL in
// siren-guard-extension/config.js (usually a Caddy hostname).
module.exports = {
  websiteServerPort: 45117,
};
