---
run: run-sirenguard-007
work_item: break-the-loop-store, idle-consequence-action
intent: break-the-loop
generated: 2026-07-13T17:56:00Z
mode: autopilot
---

# Implementation Walkthrough: Autopilot batch — store foundation + idle consequence

## Summary

Added break-the-loop configuration to `store.js` (reminder, safe app, confirmation phrase, override log, duration constants, reminder-media directory helpers) and implemented `idle-consequence-action.js` to activate a configured safe app via AppleScript then lock the screen. Exported `lockScreen` from `lock-orchestration.js` for reuse by consequence actions.

## Structure Overview

Settings flow through the existing `electron-store` wrapper. New keys are readable/writable via the same `getAllSettings` / `updateSettings` path the dashboard will use later. The idle consequence module is a thin action layer that reads `getSafeApp()`, optionally runs `osascript`, then calls `lockOrchestration.lockScreen()` — intended to be invoked by the future lock-orchestration refactor after the reminder overlay completes.

## Files Changed

### Created

| File | Purpose |
|------|---------|
| `idle-consequence-action.js` | `executeIdleConsequence()` — safe app activation + lock |
| `test/idle-consequence-action.test.js` | Unit tests with injected `execFn` mock |

### Modified

| File | Changes |
|------|---------|
| `store.js` | Defaults, constants, accessors, reminder-media helpers, settings merge |
| `lock-orchestration.js` | Export `lockScreen` |
| `test/00-store.test.js` | 9 new tests for break-the-loop store keys |

## Domain Model

| Setting | Shape | Notes |
|---------|-------|-------|
| `reminder` | `{ mediaType, mediaPath, caption, minWatchSec }` | Global reminder config |
| `safeApp` | `{ name, bundleId }` | Idle redirect target |
| `confirmationPhrase` | string | For disable/quit friction (later) |
| `overrideLog` | `{ timestamp, kind }[]` | Local override audit trail |

Constants: `RE_ENTRY_BLOCK_SEC` (300), `ESCALATION_WINDOW_SEC` (900), `MIN_WATCH_SEC` (10).

## Decisions Made

| Decision | Choice | Rationale |
|----------|--------|-----------|
| Reminder media path | Optional `userDataPath` param on dir helpers | Keeps store tests free of Electron |
| Testability of exec | Optional `execFn` on consequence functions | Bun cannot reliably mock CJS `child_process` |
| Safe app activation | Name preferred over bundle id when both set | Matches human-facing dashboard config |
| `lockScreen` export | Re-export existing function | No duplicate lock logic |

## Deviations from Plan

None — both work items implemented as specified.

## Dependencies Added

None.

## How to Verify

1. **Run unit tests**

   ```bash
   bun test
   ```

   Expected: 55 tests pass (13 new).

2. **Inspect store defaults** (optional REPL)

   ```bash
   node -e "const s=require('./store'); console.log(s.getAllSettings().reminder, s.RE_ENTRY_BLOCK_SEC)"
   ```

   Expected: reminder defaults with `minWatchSec: 10`, constants `300` / `900` / `10`.

## Test Coverage

- Tests added: 13
- Status: all passing

## Ready for Review

- [x] All acceptance criteria met
- [x] Tests passing
- [x] No critical issues
- [x] Developer notes captured

## Developer Notes

- `executeIdleConsequence()` is not wired into triggers yet — `lock-orchestration-refactor` will call it after the reminder overlay.
- `getReminderMediaDir()` without args requires Electron main process (`app.getPath`).
- Next batch run (confirm group): 5 items including `floating-button-drag-fix` (no deps).

---
*Generated by specs.md - fabriqa.ai FIRE Flow Run run-sirenguard-007*
