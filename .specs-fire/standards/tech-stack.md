# Tech Stack

## Overview

Siren Guard is a lightweight macOS menu-bar utility built with Electron. It shows a small floating, draggable button that locks the screen (via `open -a ScreenSaverEngine`) after a short arm/cancel window. No backend, database, or network layer — purely a local desktop utility.

## Core Technologies

### Language

**Primary**: JavaScript (plain, CommonJS — no TypeScript unless explicitly approved)
**Version**: ES2020+ (Node/Electron runtime defaults)

### Framework

**Framework**: Electron
**Version**: ^43.0.0
**Rationale**: Cross-window desktop shell with native tray/menu-bar integration; only realistic option for a macOS menu-bar app with a floating overlay window.

### Runtime

**Runtime**: Electron (Node.js + Chromium)
**Version**: Bundled with Electron ^43.0.0

## Data Layer

### Database

**Type**: None (local key-value persistence only)
**Database**: N/A
**Version**: N/A

### ORM / Data Access

**Tool**: `electron-store` (^11.0.2)
**Rationale**: Simple, file-backed JSON persistence for user preferences (button position, launch-at-login) with no need for a full database.

### Caching

Not configured.

## API Layer

### API Style

**Style**: Electron IPC (`ipcMain.handle` / `ipcRenderer.invoke`) via `contextBridge` in `preload.js`

### Documentation

**Tool**: None — IPC contract documented inline in `preload.js` and `main.js`

## Frontend (if applicable)

### UI Framework

**Framework**: None (plain HTML/CSS/JS renderer)
**Version**: N/A

### Styling

**Approach**: Plain CSS (`renderer/style.css`)

### State Management

**Solution**: None — minimal DOM state in `renderer/button.js`

## Infrastructure

### Hosting

**Platform**: N/A — distributed as a local macOS desktop app (not packaged/signed yet in v1)

### Containerization

Not configured.

### CI/CD

**Platform**: None configured

## Development Tools

### Package Manager

**Manager**: Bun (`bun install`, `bun start`, `bun add`) — do not use npm

### Build Tool

**Tool**: None (no bundler; files loaded directly by Electron)

### Linting

**Linter**: None configured
**Config**: N/A

### Formatting

**Formatter**: None configured (match existing indentation/style manually)
**Config**: N/A

## Dependencies

### Production Dependencies

- `electron-store` (^11.0.2) — persists button position and launch-at-login preference

### Development Dependencies

- `electron` (^43.0.0) — desktop app shell/runtime

## Version Requirements

| Tool | Minimum Version | Recommended |
|------|-----------------|-------------|
| Bun | 1.3.x | 1.3.9 |
| Electron | ^43.0.0 | ^43.0.0 |
| macOS | 12+ | Latest |

---
*Generated by specs.md - fabriqa.ai FIRE Flow*
